{
  "name": "backend",
  "version": "1.0.0",
  "description": "This is the backend part of the **Healthcare Web Application**, built with **Node.js**, **Express**, and **MongoDB**.   It handles user authentication, database operations, and serves as the main API for the frontend.",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "vite",
    "serve": "browser-sync start --proxy 'localhost:5050' --files '../frontend/**/*.*' --port 3000 --startPath 'login.html'",
    "dev-all": "concurrently \"npm run dev\" \"npm run serve\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.0.1",
    "express": "^5.1.0",
    "firebase-admin": "^13.4.0",
    "httpie": "^1.1.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.1",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^9.2.0",
    "nodemon": "^3.1.10"
  }
}
